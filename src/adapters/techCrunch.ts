// techCrunch adapter stub for Seer-AI MVP
// @cursor TASK: Implement fetch and parse logic for TechCrunch RSS feed
import type { ParsedItem } from '../types/adapter';

export async function fetchAndParse(): Promise<ParsedItem[]> {
  // @cursor TASK: Fetch and parse RSS feed, normalize, dedupe, and return ParsedItem[]
  // Example implementation should follow arxivCs.ts pattern:
  // 1. Fetch https://techcrunch.com/feed/
  // 2. Parse RSS using rss-parser
  // 3. Normalize fields to ParsedItem format
  // 4. Handle errors gracefully
  return [];
} 